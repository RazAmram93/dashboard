import DashboardSkeleton from "../../ui/skeletons";
//This is the loading comp.
//When fetching data the page will show the templets of the page without the info to show the client that the page is loading
export default function Loading(){
    return <DashboardSkeleton/>
}